import { ClickIfValidDirective } from './click-if-valid.directive';

describe('ClickIfValidDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickIfValidDirective();
    expect(directive).toBeTruthy();
  });
});
